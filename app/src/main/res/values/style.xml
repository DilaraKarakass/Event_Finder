<?xml version="1.0" encoding="utf-8"?>

<resources>
    <style name="CustomSearchView" parent="Widget.AppCompat.SearchView">
        <item name="android:textColorHint">@color/search_hint</item> <!-- Hint rengi -->
        <item name="android:textColor">@color/search_text</item> <!-- Kullanıcı yazdığında yazı rengi -->
    </style>
</resources>